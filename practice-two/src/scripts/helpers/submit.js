// Define a constant variable to represent the "Enter" key when user inputs.
export const submitInput = "Enter";

// In this case, the "submitInput" constant is used to compare against the user input value
// to determine whether they pressed the "Enter" key to submit data.
