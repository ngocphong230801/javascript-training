{
  "name": "practice-one",
  "version": "1.0.0",
  "description": "Book Marker - A web application with HTML/CSS & Javascript",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "del -rf build && del -rf .parcel-cache && parcel build ./src/*.html --no-cache --no-source-maps --dist-dir dist"
  },
  "keywords": [],
  "author": "Phong Nguyen",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}
